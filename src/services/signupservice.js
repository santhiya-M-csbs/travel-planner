/*import http from "../http-common";

const getAll = () => {
    return http.get("http://localhost:8080/endpoint");
  };
  
  
  const create = data => {
    return http.post("http://localhost:8080/endpoint/create-user", data);
  };

  const SignUpService = {
    getAll,
    create
  };
  export default SignUpService;
  */